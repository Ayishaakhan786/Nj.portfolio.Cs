import styles from "./About.module.css"; // Importing the CSS module

export default function About() {
  return (
    <div className={styles.aboutPage}>
      <h1 className={styles.aboutTitle}>I'm About Page!</h1>
    </div>
  );
}
