import styles from "./footer.module.css"; // Importing the CSS module

export default function Footer() {
  return (
    <div className={styles.footer}>
      <span>@AK Soft Tech</span>
    </div>
  );
}
