import styles from "./contact.module.css"; // Importing the CSS module

export default function Contact() {
  return (
    <div className={styles.contactPage}>
      <h1 className={styles.contactTitle}>You can Contact-us from here!</h1>
    </div>
  );
}
